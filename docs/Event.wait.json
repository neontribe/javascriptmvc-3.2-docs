c({"name": "Event.wait", "type": "attribute", "parents": ["Event"], "comment": "<p>if ( delta<em>wait &amp;&amp; sleep &lt;= delta</em>wait ) {\n                //TODO: why waste a check on a tight\n                // loop if it just falls through?\n            // if they will happen within the next delta, fall through to sleep\n            } else </p>", "src": "./steal/rhino/env.js", "line": 6974, "children": []})